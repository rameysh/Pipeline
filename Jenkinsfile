pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', https://github.com/rameysh/Pipeline.git) 
            }
           }
   
        stage('done') {
            step
            }  
        }
    }
}
