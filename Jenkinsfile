pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/rameysh/Pipeline.git' 
            }
           }
   
        stage2('done') {
            step
            }  
        }
    }
}
